[package]
name = "web3-suite-identity-credentials"
version = "0.1.0"
edition = "2021"
description = "Verifiable Credentials Soroban Smart Contract"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "21.0.0"

[dev-dependencies]
soroban-sdk = { version = "21.0.0", features = ["testutils"] }
